################################################################################
# Files are splitted for clarity (into ~/.zsh/*.zsh)
################################################################################

for i in `find ~/.zsh/ -maxdepth 1 -name '*.zsh' -type f` ; do
  source $i
done
