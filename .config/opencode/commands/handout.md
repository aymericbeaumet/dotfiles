---
description: Create a handout or load one by ID
---

Use the `handout` skill. If `$ARGUMENTS` is empty, create a new handout and report its generated ID.
Otherwise load the handout whose ID is exactly `$ARGUMENTS` and continue from it.
