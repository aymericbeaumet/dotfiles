# Lazy loading (once)
${__pyenv_loaded:-false} || {

  export PYENV_ROOT="$HOME/.pyenv"
  if which pyenv &> /dev/null; then
    eval "$(command pyenv init -)"
  fi

__pyenv_loaded=true }

# Run
case "$(type -p "$0")" in
  *not\ found) "$0" "$@" ;;
  *) command "$0" "$@" ;;
esac
